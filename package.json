{
	"name": "laravel-bootstrap-blade-preset",
	"license": "MIT",
	"preset": "src/preset.ts",
	"version": "0.0.0",
	"private": "true",
	"devDependencies": {
		"apply": "^0.2.12"
	},
	"scripts": {
		"release": "standard-version --skip.changelog"
	}
}
